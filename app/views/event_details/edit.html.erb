<%= form_for @detail, :url => event_event_detail_path(@event) do |f| %>

  <%= f.text_area :information, class: "form-control",:rows => "3"%>
  <%= f.submit "送出", :class => "btn btn-primary"%>

<% end %>