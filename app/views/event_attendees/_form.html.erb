<% if @attendee.errors.any? %>
      <ul>
      <% @attendee.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
<% end %>

  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.submit "送出", :class => "btn btn-primary" %>